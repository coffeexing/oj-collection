#include <iostream>
#include <vector>
using namespace std;

struct State {
	int time;
	vector<bool> todo;
};

int main() {
	int s1, s2, s3, s4;
	scanf("%d%d%d%d", &s1, &s2, &s3, &s4);
	vector<int> AT(s1);
	vector<bool> AD(s1, false);
	
	
	return 0;
}
